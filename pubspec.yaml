name: filigran_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Durum yönetimi
  flutter_riverpod: ^2.4.9
  hooks_riverpod: ^2.4.9
  flutter_hooks: ^0.20.3
  riverpod_annotation: ^2.3.3
  # Model ve kod üretimi
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  # Resim işlemleri
  image_picker: ^1.0.5
  image: ^4.1.3
  path_provider: ^2.1.5
  permission_handler: ^11.0.1
  # UI bileşenleri
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  flutter_reactive_value: ^1.0.0
  share_plus: ^10.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.9

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
